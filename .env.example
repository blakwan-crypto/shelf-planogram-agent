# Copy this file to ".env" and fill in real values. NEVER commit .env.
# The baseline pipeline is rule-based and needs no API keys.
# Add keys here only if you enable optional LLM/VLM features later.

# OPENAI_API_KEY=sk-...
# ANTHROPIC_API_KEY=sk-ant-...
